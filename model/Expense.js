export class Expense {
    constructor(id, name, amount, selectedParticipants) {
        this.id = id;
        this.name = name;
        this.amount = amount;
        this.selectedParticipants = selectedParticipants
    }
}